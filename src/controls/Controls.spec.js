// Test away!
/*provide buttons to toggle the closed and locked states.
buttons' text changes to reflect the state the door will be in if clicked
the closed toggle button is disabled if the gate is closed
the locked toggle button is disabled if the gate is open */

// import React from 'react';
// import * as rt from 'react-testing-library';
// import Controls from './Controls';

// // describe('Controls Component', () => {
// //   it('toggles state of locked button', () => {
// //     const wrap = rt.render(<Controls locked={true} />);
// //     rt.fireEvent.click(wrap.queryAllByTestId(/lockButton/i));
// //     wrap.getAllByText(/unlock/i);
// //   });
// // });
